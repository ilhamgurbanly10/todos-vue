import type { TodoForm } from "./types";

export const initialFormData: TodoForm = {
     name: '',
        description: '',
        deadline: null,
        start_date: null,
        status: undefined,
}